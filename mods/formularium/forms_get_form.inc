<?php
  //////////////////////////////////////////////////////////////////////////////
  // Выводит форму на отдельной странице в пользовательской части сайта
  //////////////////////////////////////////////////////////////////////////////


  //инициализация переменных
  $id_form = intval($_GET['id_form']);

  //заголовок формы
  $query = "SELECT title FROM {$gl_db_prefix}forms WHERE id_form = {$id_form}";
  $f_title = stripslashes(db_select_one($query));

  print "<h1>{$f_title}</h1>";

  //выводим форму
  forms_create_form($id_form);
?>