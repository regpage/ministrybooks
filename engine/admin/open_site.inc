<?php
  /////////////////////////////////////////////////////////////////////////////
  // ќсновные узлы системы
  //////////////////////////////////////////////////////////////////////////////

  //////////////////////////////////////////////////////////////////////////////
  // ћодуль создает в папке config файл "site_is_locked"
  // ≈го наличие указывает на то, что сайт закрыт дл€ посетителей.
  //////////////////////////////////////////////////////////////////////////////

  //проверка прав доступа
  if($gl_group != 'admin')
    return;

  //создаем файл, наличие которого указывает на то, что сайт закрыт дл€ посетителей
  $lock_file = "{$gl_path_to_root}config/site_is_locked.inc";
  if(file_exists($lock_file))
    unlink($lock_file);

  //осуществл€ем переход на главную страницу системы администрировани€.
  echo "<HTML><HEAD><META HTTP-EQUIV='Refresh' CONTENT='0; URL=?adm=main'></HEAD></HTML>";
  exit;



?>