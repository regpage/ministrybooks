<?php
  //////////////////////////////////////////////////////////////////////////////
  // Система управления пользователями
  //////////////////////////////////////////////////////////////////////////////

  //////////////////////////////////////////////////////////////////////////////
  //Модуль выдает сообщение об успешной регистрации
  //////////////////////////////////////////////////////////////////////////////

  $id_user = intval($_GET['id_user']);

  if($site_users_activation == 'yes')
  {
    $email = stripslashes(db_select_one("SELECT email FROM {$gl_db_prefix}users WHERE id_user = {$id_user}"));
    $mess  = str_replace("{email}", $email, $l_users[46]);
    print $mess;
  }
  else
    print $l_users[22];
?>